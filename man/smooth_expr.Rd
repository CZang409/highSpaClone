% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{smooth_expr}
\alias{smooth_expr}
\title{Smooth expression along chromosomes (windowed running mean) with optional chunking/parallelism.}
\usage{
smooth_expr(
  obj,
  window_size = 101,
  step = 50,
  exclude_chromosomes = c("chrX", "chrY", "chrM"),
  smooth_with_ends = FALSE,
  use_chunk = TRUE,
  chunk_size = 5000,
  parallel = FALSE,
  n_cores = NULL,
  max_cores = 4
)
}
\arguments{
\item{obj}{highSpaClone object.}

\item{window_size}{Integer. Size of the running-mean window (in genes) used within each chromosome (default: 101).}

\item{step}{Integer. Number of genes in a bin (default: 50).}

\item{exclude_chromosomes}{Chromosomes that are excluded from analysis (default: c('chrX', 'chrY', 'chrM')).}

\item{smooth_with_ends}{Logical. Whether to apply an end-aware smoother (default: \code{FALSE}).}

\item{use_chunk}{Logical. If \code{TRUE}, split cells into chunks of size \code{chunk_size}}

\item{chunk_size}{Integer. Number of cells per chunk when \code{use_chunk=TRUE}.}

\item{parallel}{Logical. If \code{TRUE}, process chunks in parallel.}

\item{n_cores}{Integer or \code{NULL}. Number of CPU cores to use when \code{parallel=TRUE}.
If \code{NULL}, uses \code{parallel::detectCores()-1}. The effective cores are capped
by \code{max_cores} and the number of chunks (default \code{NULL}).}

\item{max_cores}{Integer. Upper bound on cores even if more are available (default \code{4}).}
}
\value{
Return highSpaClone object containing smoothed gene expression data
}
\description{
Smooth expression along chromosomes (windowed running mean) with optional chunking/parallelism.
}
